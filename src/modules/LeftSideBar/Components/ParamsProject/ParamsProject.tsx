import { observer } from "mobx-react-lite";
import React from "react";
import { SidebarName } from "src/UI";

export const ParamsProject = observer(() => {
  return (
    <div>
      <SidebarName>Params</SidebarName>
    </div>
  );
});

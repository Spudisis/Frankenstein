import styled from "styled-components";

export const Wrapper = styled.div`
  position: relative;
  padding: 20px 0px;
  label:first-child {
    display: flex;
    flex-direction: column;
    color: white;
    font-size: 22px;
    input {
      border-radius: 10px;
      padding: 5px 10px;
    }
  }
`;
